# API keys are stored in localStorage at runtime — these env vars are NOT used by the app.
# They are provided here as reference only, for contributors running a local backend (Phase 4).
# Never commit actual keys.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
